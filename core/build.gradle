apply(plugin: "info.solidsoft.pitest");

dependencies {
    compile project(":grappa-debugger-db");
}

description = "GUI application to debug grappa/parboiled 1.x parsers; core code";

install.enabled = false;
uploadArchives.enabled = false;
