include "core", "javafx", "web";

/*
 * We STILL have to do that!
 */

project(":core").name = "grappa-debugger-core";
project(":javafx").name = "grappa-debugger-javafx";
project(":web").name = "grappa-debugger-web";
